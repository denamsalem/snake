export class View {
    constructor(domElements) {
        this.domElements = domElements
    }
    updateScore(playerScore, cpuScore) {}
    hideIcons() {}
    hide(element) {}
    show(elementName) {}
} 